<?xml version="1.0" encoding="UTF-8"?><rss xmlns:dc="http://purl.org/dc/elements/1.1/" xmlns:content="http://purl.org/rss/1.0/modules/content/" xmlns:atom="http://www.w3.org/2005/Atom" version="2.0"><channel><title><![CDATA[port]]></title><description><![CDATA[port]]></description><link>http://kicz.me</link><generator>RSS for Node</generator><lastBuildDate>Sun, 10 Jun 2018 01:18:48 GMT</lastBuildDate><atom:link href="http://kicz.me/feed.xml" rel="self" type="application/rss+xml"/><item><title><![CDATA[Angular4 and AngularFire2 OAuth]]></title><description><![CDATA[<h1 id="the-complete-guide-to-angularfire-oauth">The complete guide to AngularFire OAuth</h1>
<p>In the never ending quest to keep up with the rapidly evolving JS framework brawl, I've been learning Google's Angular, one of the most popular and powerful frameworks in the JavaScript world. One of the first things one needs to think about when starting any complex web app is how to add secure user accounts for users to act through. After lots of searching and finding little but outdated tutorials and spotty documentation, I've compiled all the knowledge I've gained about how to create an extremely user friendly, secure, and simple authentication service (the Google way) using the latest versions of Angular, AngularFire2, and Material2. What took me a couple days of work should now take no more than an hour!</p>
<p>So why use AngularFire OAuth instead of creating your own authentication using something like <a href="https://en.wikipedia.org/wiki/JSON_Web_Token">JWT</a> (JSON Web Tokens)? First of all, unless you're a seasoned cyber security specialist, it's generally considered the most secure approach to not try to roll your own authentication system. The exception being if you're just making a fun side project for the sake of learning that will never handle real production loads. Using authentication services such as OAuth leaves critical security implementation up to the experts and allows you to focus on other major features of your app. Enabling common OAuth providers such as Google, Facebook, and Twitter also creates a lower barrier to entry and better user experience because users don't need to worry about signing up for a whole new service and verifying their email. They may not even need to enter password credentials if they're already logged into their chosen service.</p>
<p>There's a number of other great benefits to using Firebase for not only your app's authentication but as your entire backend service and I'd highly suggest you read <a href="http://blog.angular-university.io/angular-2-firebase/">this great aticle</a> about how Angular and Firebase are changing the web dev world.</p>
<h1 id="step-1-create-firebase-app-and-activate-oauth-providers">Step 1: Create Firebase App and Activate OAuth Providers</h1>
<h3 id="create-a-firebase-account-and-app">Create a Firebase Account and App</h3>
<p>The first step is to <a href="https://accounts.google.com/signin/v2/identifier?continue=https%253A%252F%252Fconsole.firebase.google.com%252F&#x26;followup=https%253A%252F%252Fconsole.firebase.google.com%252F&#x26;osid=1&#x26;flowName=GlifWebSignIn&#x26;flowEntry=ServiceLogin">create</a> a Firebase account if you haven't already.</p>
<p>Then create your new app from the Firebase console and select 'Authentication' from the side nav and go to 'sign-in-method'. Here you'll see all the sign-in providers you can enable with through Firebase. You can select whatever providers you want, but this tutorial will only cover Google, Facebook, and Github for now.</p>
<p><img src="../../assets/sitePics/firebase.jpg" alt="sign in providers"></p>
<p>Google is the easiest, all you have to do is select enable  and you're done.</p>
<p>For Facebook you'll have to create a new app from their <a href="https://developers.facebook.com/">developer page</a> and provide the Callback URL provided by Firebase to generate your App ID and App secret tokens.</p>
<p>You'll have to do the same for Github at the <a href="https://github.com/settings/applications">application settings page</a> It'll ask you for the homepage of your website, but you can put anything here if your app doesn't have one yet. Then just enter the Firebase callback URL again and you'll now have your access tokens to fill in the blanks on Firebase</p>
<h3 id="create-config-file-for-firebase-configuration">Create Config File for Firebase Configuration</h3>
<p>Now we're ready to start configuring Firebase in our app. Navigate to your app's Overview panel and select 'Add Firebase to your web app' and copy the config properties into a new file using the template below.</p>
<h4 id="configts">config.ts</h4>
<pre><code class="hljs language-ts"><span class="hljs-keyword">export</span> <span class="hljs-keyword">const</span> firebaseConfig = {
  apiKey: <span class="hljs-string">'your-key'</span>,
  authDomain: <span class="hljs-string">'your-app-name.firebaseapp.com'</span>,
  databaseURL: <span class="hljs-string">'https://your-app-name.firebaseio.com'</span>,
  storageBucket: <span class="hljs-string">'your-app-name.appspot.com'</span>,
  messagingSenderId: <span class="hljs-string">'your-messaging-sender-id'</span>
};</code></pre>
<p>We could have put this config object directly in our app.module.ts file but then we wouldn't be able to exclude its very sensitive details from being commited if using Github. This file becomes a handy little 'secret' repository for any variables we wish to keep from prying eyes.</p>
<h4 id="gitignore">.gitignore</h4>
<pre><code class="hljs language-ts">config.ts</code></pre>
<p>Make sure to add the new config file to your gitignore file so you don't accidentally upload your app's security details for all to see.</p>
<h4 id="appmodulets">app.module.ts</h4>
<pre><code class="hljs language-ts"><span class="hljs-keyword">import</span> { firebaseConfig } from <span class="hljs-string">'../config'</span>;

...

imports: [
  AngularFireModule.initializeApp(firebaseConfig),
]</code></pre>
<p>Remember to import our firebase config object and initialize the AngularFireModule with it.</p>
<h1 id="step-2-installation-and-component-generation">Step 2: Installation and Component Generation</h1>
<pre><code class="hljs language-console"> npm <span class="hljs-keyword">install</span> firebase angularfire2 <span class="hljs-comment">--save</span></code></pre>
<p>Here we instruct NPM to install both firebase and angularfire2 to our node_modules folder and save them as dependencies in our package.json file.</p>
<pre><code class="hljs language-console"><span class="hljs-attribute"> ng g service auth</span></code></pre>
<p>Now we use the Angular CLI shorthand 'g' for 'generate' and instruct Angular to create a new service to be injected into our app called auth.  </p>
<pre><code class="hljs language-console"> <span class="hljs-attribute">ng</span> g component users/login</code></pre>
<pre><code class="hljs language-console"> <span class="hljs-attribute">ng</span> g component users/profile</code></pre>
<p>The convenient generate feature of the Angular CLI  automatically creates the user folder, files, imports,  declarations, and other boilerplate necessary for our new service and components, so we're ready to start adding our business logic right away!</p>
<p>These next steps are optional but it'll be handy to install Material2 and FontAwesome to help style our components later. You could use another styling library like ng-bootstrap but Google's Material2 has a lot of nice built in animations and philosophy guiding its components and will provide a better user experience. As of this writing, it's only in Beta and the docs are still under construction, but expect it to be the most compatible styling library for Angular.</p>
<h3 id="install-material2-and-flexbox-optional">Install Material2 and Flexbox (Optional)</h3>
<pre><code class="hljs language-console"> <span class="hljs-built_in">npm</span> install @angular/material @angular/flex-layout --save</code></pre>
<p>We'll only need the <a href="https://github.com/angular/flex-layout">flex layout module</a> to make our Navbar responsive but this Angular Module that injects the handy Flexbox has a number of other handy features that will be convienent for the layout of your app.</p>
<h4 id="appmodulets-1">app.module.ts</h4>
<pre><code class="hljs language-ts"><span class="hljs-keyword">import</span> {
  MdButtonModule,  
  MdMenuModule,
  MdToolbarModule,
  MdIconModule,
  MdInputModule
   } from <span class="hljs-string">'@angular/material'</span>;
<span class="hljs-keyword">import</span> { FlexLayoutModule } from <span class="hljs-string">"@angular/flex-layout"</span>;

...

imports: [
  MdButtonModule,
  MdMenuModule,
  MdToolbarModule,
  MdIconModule,
  MdInputModule,
  FlexLayoutModule,
]</code></pre>
<h4 id="indexhtml">index.html</h4>
<pre><code class="hljs language-ts">&#x3C;link href=<span class="hljs-string">"https://fonts.googleapis.com/icon?family=Material+Icons"</span> rel=<span class="hljs-string">"stylesheet"</span>>

&#x3C;link href=<span class="hljs-string">"../node_modules/@angular/material/prebuilt-themes/indigo-pink.css"</span> rel=<span class="hljs-string">"stylesheet"</span>></code></pre>
<p>We must include the actual icon assets, the full list of which can be found <a href="https://material.io/icons/">here</a></p>
<p>And we include a default Material theme that is required for the styling of our components. More info about Material theming can be found <a href="https://material.angular.io/guide/theming">here</a></p>
<h3 id="install-font-awesome-optional">Install Font Awesome (Optional)</h3>
<pre><code class="hljs language-ts"> npm install font-awesome angular2-fontawesome --save</code></pre>
<p>Now we install the Angular Font Awesome libraries so we can add the social icons (that Material2 is missing) to our buttons. If you're super concerned with keeping your app's file size to a minimum you might want to take the time to find and download individual icon assets for the specific services you want.</p>
<h4 id="angular-clijson">.angular-cli.json</h4>
<pre><code class="hljs language-ts">{
  <span class="hljs-string">"apps"</span>: [
    {
      <span class="hljs-string">"styles"</span>: [
        <span class="hljs-string">"../node_modules/font-awesome/css/font-awesome.css"</span>
      ]
    }
  ],
  <span class="hljs-string">"addons"</span>: [
    <span class="hljs-string">"../node_modules/font-awesome/fonts/*.+(otf|eot|svg|ttf|woff|woff2)"</span>
  ]
}</code></pre>
<p>Here we tell our app where and how to consume our new FA icons in the angular CLI config file (which is just an Angular wrapper of a Webpack2 config file)</p>
<h4 id="appmodulets-2">app.module.ts</h4>
<pre><code class="hljs language-ts"><span class="hljs-keyword">import</span> { Angular2FontawesomeModule } from <span class="hljs-string">'angular2-fontawesome/angular2-fontawesome'</span>

...

imports: [
  Angular2FontawesomeModule,
]</code></pre>
<h1 id="step-3-creating-the-backend-authentication-service">Step 3: Creating the Backend Authentication Service</h1>
<p>Yay! We're finally ready to get to the core business logic of our authentication system. This AuthService component does all the core functions that tie our app user's authentication to Firebase.</p>
<h4 id="authservicets">auth.service.ts</h4>
<pre><code class="hljs language-ts"><span class="hljs-keyword">import</span> { Injectable } from <span class="hljs-string">'@angular/core'</span>;
<span class="hljs-keyword">import</span> {
  AngularFireDatabaseModule,
  AngularFireDatabase,
  FirebaseListObservable } from <span class="hljs-string">'angularfire2/database'</span>;
<span class="hljs-keyword">import</span> { AngularFireAuth } from <span class="hljs-string">'angularfire2/auth'</span>;
<span class="hljs-keyword">import</span> { Router } from <span class="hljs-string">"@angular/router"</span>;
<span class="hljs-keyword">import</span> * as firebase from <span class="hljs-string">'firebase'</span>;</code></pre>
<p>First we import all our dependencies, using AngularFireDatabase modules so that we can store our user's information for later use and to access the Observable function that we'll use to subscribe to our user's login status.</p>
<pre><code class="hljs language-ts"><span class="hljs-meta">@Injectable</span>()
<span class="hljs-keyword">export</span> <span class="hljs-keyword">class</span> AuthService {

  authState: <span class="hljs-built_in">any</span> = <span class="hljs-literal">null</span>;

  <span class="hljs-keyword">constructor</span>(<span class="hljs-params"><span class="hljs-keyword">private</span> afAuth: AngularFireAuth,
              <span class="hljs-keyword">private</span> db: AngularFireDatabase,
              <span class="hljs-keyword">private</span> router:Router</span>) {

            <span class="hljs-keyword">this</span>.afAuth.authState.subscribe((auth) => {
              <span class="hljs-keyword">this</span>.authState = auth
            });
          }</code></pre>
<p>Then we pass in the AngularFire objects we need as private variables to our class constructor. The private specification simply means that these objects can only be accessed and used within the scope of our AuthService class.</p>
<p>The above code is the most critical step in the Auth service because it subscribes to the AngularFire authState observable and returns a FirebaseAuthState object with details on our user's auth state that will allow us to determine when to show, hide, and guard application details.</p>
<p>When the user is logged out the state is null and when logged in, the object contains useful user details like User ID, Display Name, Profile Pic URL, etc.</p>
<pre><code class="hljs language-ts">  <span class="hljs-comment">// Returns true if user is logged in</span>
  <span class="hljs-keyword">get</span> authenticated(): <span class="hljs-built_in">boolean</span> {
    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.authState !== <span class="hljs-literal">null</span>;
  }

  <span class="hljs-comment">// Returns current user data</span>
  <span class="hljs-keyword">get</span> currentUser(): <span class="hljs-built_in">any</span> {
    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.authenticated ? <span class="hljs-keyword">this</span>.authState : <span class="hljs-literal">null</span>;
  }

  <span class="hljs-comment">// Returns an observable of our user status</span>
  <span class="hljs-keyword">get</span> currentUserObservable(): <span class="hljs-built_in">any</span> {
    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.afAuth.authState
  }

  <span class="hljs-comment">// Returns current user UID</span>
  <span class="hljs-keyword">get</span> currentUserId(): <span class="hljs-built_in">string</span> {
    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.authenticated ? <span class="hljs-keyword">this</span>.authState.uid : <span class="hljs-string">''</span>;
  }

  <span class="hljs-comment">// Returns current user display name or Guest</span>
  <span class="hljs-keyword">get</span> currentUserDisplayName(): <span class="hljs-built_in">string</span> {
    <span class="hljs-keyword">if</span> (!<span class="hljs-keyword">this</span>.authState) { <span class="hljs-keyword">return</span> <span class="hljs-string">'Guest'</span> }
    <span class="hljs-keyword">else</span> { <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.authState[<span class="hljs-string">'displayName'</span>] || <span class="hljs-string">'User without a Name'</span> }
  }</code></pre>
<p>Now using getters we set up a number of useful helper functions that we can use in our app. Since classes in JS (ES6) and TS are just syntatic sugar for the standard prototypal inheritance model of objects in JS. Accessors like getters and setters make functions (in this case methods) behave more like object properties and give you more control over member attributes for class. For instance, getters specify what a function should run and return when called as a property (e.g. this.exampleFunc), while setters specify how functions should react to inputs from initialization (e.g. this.exampleFunc = value)</p>
<h3 id="social-sign-in">Social Sign In</h3>
<pre><code class="hljs language-ts">  <span class="hljs-comment">//// Social Auth ////</span>

  githubLogin() {
    <span class="hljs-keyword">const</span> provider = <span class="hljs-keyword">new</span> firebase.auth.GithubAuthProvider()
    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.socialSignIn(provider);
  }

  googleLogin() {
    <span class="hljs-keyword">const</span> provider = <span class="hljs-keyword">new</span> firebase.auth.GoogleAuthProvider()
    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.socialSignIn(provider);
  }

  facebookLogin() {
    <span class="hljs-keyword">const</span> provider = <span class="hljs-keyword">new</span> firebase.auth.FacebookAuthProvider()
    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.socialSignIn(provider);
  }

  <span class="hljs-keyword">private</span> socialSignIn(provider) {
    <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.afAuth.auth.signInWithPopup(provider)
      .then((credential) =>  {
        <span class="hljs-keyword">this</span>.authState = credential.user
        <span class="hljs-keyword">this</span>.updateUserData()
      })
      .catch(error => <span class="hljs-built_in">console</span>.log(error));
  }

  <span class="hljs-comment">//// Sign Out ////</span>

  signOut(): <span class="hljs-built_in">void</span> {
    <span class="hljs-keyword">this</span>.afAuth.auth.signOut();
    <span class="hljs-keyword">this</span>.router.navigate([<span class="hljs-string">'/'</span>])
  }</code></pre>
<p>Here we create a reusable private function that takes the login provider as an argument and attempts to sign in through a pop up window. Once the user has entered their information, it will either be a success and then their login credentials are passed to our updateUserData function to be stored in our Firebase database. Or if there's an error, we catch it and log it to our console. (We could also provide some user feedback here)</p>
<p>We also set up a signOut function that simply logs the user out and redirects them to our home page.</p>
<pre><code class="hljs language-ts">  <span class="hljs-comment">//// Helpers ////</span>

  <span class="hljs-keyword">private</span> updateUserData(): <span class="hljs-built_in">void</span> {
  <span class="hljs-comment">// Writes user name and email to realtime db</span>
  <span class="hljs-comment">// useful if your app displays information about users or for admin features</span>

    <span class="hljs-keyword">let</span> path = <span class="hljs-string">`users/<span class="hljs-subst">${this.currentUserId}</span>`</span>; <span class="hljs-comment">// Endpoint on firebase</span>
    <span class="hljs-keyword">let</span> data = {
                  email: <span class="hljs-keyword">this</span>.authState.email,
                  name: <span class="hljs-keyword">this</span>.authState.displayName
                }

    <span class="hljs-keyword">this</span>.db.object(path).update(data)
    .catch(error => <span class="hljs-built_in">console</span>.log(error));
  }
}</code></pre>
<p>Here's the optional helper function that saves our user data to our realtime Firebase database. This could be very useful later for administrative features and collecting more data on users.</p>
<h1 id="step-4-creating-the-user-login-component">Step 4: Creating the User Login Component</h1>
<h4 id="usersloginlogincomponentts">users/login/login.component.ts</h4>
<pre><code class="hljs language-ts"><span class="hljs-keyword">import</span> { Component, OnInit, HostBinding } from <span class="hljs-string">'@angular/core'</span>;
<span class="hljs-keyword">import</span> { Router } from <span class="hljs-string">'@angular/router'</span>;
<span class="hljs-keyword">import</span> { AngularFireAuth } from <span class="hljs-string">'angularfire2/auth'</span>;
<span class="hljs-comment">// Do not import from 'firebase' as you'd lose the tree shaking benefits</span>
<span class="hljs-keyword">import</span> * as firebase from <span class="hljs-string">'firebase/app'</span>
<span class="hljs-keyword">import</span> { AuthService } from <span class="hljs-string">"../../auth.service"</span>;

<span class="hljs-meta">@Component</span>({
  selector: <span class="hljs-string">'app-login'</span>,
  templateUrl: <span class="hljs-string">'./login.component.html'</span>,
  styleUrls: [<span class="hljs-string">'./login.component.css'</span>]
})
<span class="hljs-keyword">export</span> <span class="hljs-keyword">class</span> LoginComponent <span class="hljs-keyword">implements</span> OnInit {

  <span class="hljs-keyword">constructor</span>(<span class="hljs-params"><span class="hljs-keyword">public</span> auth: AuthService,
              <span class="hljs-keyword">private</span> router: Router</span>) { }

  <span class="hljs-keyword">private</span> afterSignIn(): <span class="hljs-built_in">void</span> {
    <span class="hljs-comment">// Do after login stuff here, such router redirects, toast messages, etc.</span>
    <span class="hljs-keyword">this</span>.router.navigate([<span class="hljs-string">'/profile'</span>]);
  }

  signInWithGithub(): <span class="hljs-built_in">void</span> {
    <span class="hljs-keyword">this</span>.auth.githubLogin()
    .then(() => <span class="hljs-keyword">this</span>.afterSignIn());
  }
  signInWithGoogle(): <span class="hljs-built_in">void</span> {
    <span class="hljs-keyword">this</span>.auth.googleLogin()
      .then(() => <span class="hljs-keyword">this</span>.afterSignIn());
  }
  signInWithFacebook(): <span class="hljs-built_in">void</span> {
    <span class="hljs-keyword">this</span>.auth.facebookLogin()
      .then(() => <span class="hljs-keyword">this</span>.afterSignIn());
  }

  ngOnInit() { }
}</code></pre>
<p>Now we can start setting up our login component where we import our Auth Service that we just created to access our accessor OAuth functions. Here we're using promises as a nice abstraction to specify the callbacks fired after we sign in with our auth service functions. Here we're just routing our users to the profile page (that we'll create later) so they can see their user info and that they're signed in. This could obviously be changed to whatever you please.</p>
<h4 id="usersloginlogincomponenthtml">users/login/login.component.html</h4>
<pre><code class="hljs language-html"><span class="hljs-tag">&#x3C;<span class="hljs-name">h1</span>></span>Login Providers:<span class="hljs-tag">&#x3C;/<span class="hljs-name">h1</span>></span>
<span class="hljs-tag">&#x3C;<span class="hljs-name">div</span> <span class="hljs-attr">id</span>=<span class="hljs-string">"signin-buttons"</span>></span>
  <span class="hljs-tag">&#x3C;<span class="hljs-name">span</span> <span class="hljs-attr">class</span>=<span class="hljs-string">"error"</span> *<span class="hljs-attr">ngIf</span>=<span class="hljs-string">"error"</span>></span>{{ error }}<span class="hljs-tag">&#x3C;/<span class="hljs-name">span</span>></span>
  <span class="hljs-tag">&#x3C;<span class="hljs-name">div</span> *<span class="hljs-attr">ngIf</span>=<span class="hljs-string">"!auth.currentUser; else alreadyLoggedIn"</span>></span>
  <span class="hljs-tag">&#x3C;<span class="hljs-name">button</span> <span class="hljs-attr">md-raised-button</span> (<span class="hljs-attr">click</span>)=<span class="hljs-string">"signInWithFacebook()"</span> <span class="hljs-attr">class</span>=<span class="hljs-string">"login"</span> <span class="hljs-attr">color</span>=<span class="hljs-string">"primary"</span>></span><span class="hljs-tag">&#x3C;<span class="hljs-name">fa</span> [<span class="hljs-attr">name</span>]=<span class="hljs-string">"'facebook'"</span>></span><span class="hljs-tag">&#x3C;/<span class="hljs-name">fa</span>></span> Login With Facebook<span class="hljs-tag">&#x3C;/<span class="hljs-name">button</span>></span>
  <span class="hljs-tag">&#x3C;<span class="hljs-name">button</span> <span class="hljs-attr">md-raised-button</span> (<span class="hljs-attr">click</span>)=<span class="hljs-string">"signInWithGoogle()"</span> <span class="hljs-attr">class</span>=<span class="hljs-string">"login"</span> <span class="hljs-attr">color</span>=<span class="hljs-string">"primary"</span>></span><span class="hljs-tag">&#x3C;<span class="hljs-name">fa</span> [<span class="hljs-attr">name</span>]=<span class="hljs-string">"'google'"</span>></span><span class="hljs-tag">&#x3C;/<span class="hljs-name">fa</span>></span> Login With Google<span class="hljs-tag">&#x3C;/<span class="hljs-name">button</span>></span>
  <span class="hljs-tag">&#x3C;<span class="hljs-name">button</span> <span class="hljs-attr">md-raised-button</span> (<span class="hljs-attr">click</span>)=<span class="hljs-string">"signInWithGithub()"</span> <span class="hljs-attr">class</span>=<span class="hljs-string">"login"</span> <span class="hljs-attr">color</span>=<span class="hljs-string">"primary"</span>></span><span class="hljs-tag">&#x3C;<span class="hljs-name">fa</span> [<span class="hljs-attr">name</span>]=<span class="hljs-string">"'github'"</span>></span><span class="hljs-tag">&#x3C;/<span class="hljs-name">fa</span>></span> Login With Github<span class="hljs-tag">&#x3C;/<span class="hljs-name">button</span>></span>
<span class="hljs-tag">&#x3C;/<span class="hljs-name">div</span>></span>

<span class="hljs-tag">&#x3C;<span class="hljs-name">ng-template</span> #<span class="hljs-attr">alreadyLoggedIn</span>></span>
    already logged in!
<span class="hljs-tag">&#x3C;/<span class="hljs-name">ng-template</span>></span></code></pre>
<p>Now we create the view for the login component. We use the <a href="https://angular.io/docs/ts/latest/api/common/index/NgIf-directive.html">ngIf</a> Angular directive to conditionally render elements for any errors that might occur while signing in. We also conditionally set the template with the list of provider sign-in buttons to be conditionally displayed if there is no current user, or else we display a template that simply says 'already logged in!' if the user navigates back to the login page after signing in.</p>
<h1 id="step-5-creating-the-user-profile-component">Step 5: Creating the User Profile Component</h1>
<h4 id="usersprofileprofilecomponentts">users/profile/profile.component.ts</h4>
<pre><code class="hljs language-ts"><span class="hljs-keyword">import</span> { Component, OnInit } from <span class="hljs-string">'@angular/core'</span>;
<span class="hljs-keyword">import</span> { AuthService } from <span class="hljs-string">"../../auth.service"</span>;

<span class="hljs-meta">@Component</span>({
  selector: <span class="hljs-string">'app-profile'</span>,
  templateUrl: <span class="hljs-string">'./profile.component.html'</span>,
  styleUrls: [<span class="hljs-string">'./profile.component.css'</span>]
})
<span class="hljs-keyword">export</span> <span class="hljs-keyword">class</span> ProfileComponent <span class="hljs-keyword">implements</span> OnInit {

  <span class="hljs-keyword">constructor</span>(<span class="hljs-params"><span class="hljs-keyword">public</span> auth: AuthService</span>) { }

  logout() {
    <span class="hljs-keyword">this</span>.auth.signOut();
  }

  ngOnInit() {
  }

}</code></pre>
<p>The profile component's class is very simple, just a logout function that we'll use to attach to the logout button in the profile view.</p>
<h4 id="usersprofileprofilecomponenthtml">users/profile/profile.component.html</h4>
<pre><code class="hljs language-html">  <span class="hljs-comment">&#x3C;!-- User logged in --></span>
  <span class="hljs-tag">&#x3C;<span class="hljs-name">div</span> *<span class="hljs-attr">ngIf</span>=<span class="hljs-string">"auth.currentUser"</span>></span>
    <span class="hljs-tag">&#x3C;<span class="hljs-name">h3</span>></span>Hi there, {{ auth.currentUserDisplayName }}<span class="hljs-tag">&#x3C;/<span class="hljs-name">h3</span>></span>
    <span class="hljs-tag">&#x3C;<span class="hljs-name">img</span> [<span class="hljs-attr">src</span>]=<span class="hljs-string">"auth.currentUser.photoURL || 'https://api.adorable.io/avatars/109/fire.png'"</span> <span class="hljs-attr">id</span>=<span class="hljs-string">"account-img"</span> <span class="hljs-attr">class</span>=<span class="hljs-string">"img-circle"</span>></span>
    <span class="hljs-tag">&#x3C;<span class="hljs-name">br</span>></span>
    <span class="hljs-tag">&#x3C;<span class="hljs-name">button</span> <span class="hljs-attr">md-raised-button</span> <span class="hljs-attr">color</span>=<span class="hljs-string">"primary"</span> (<span class="hljs-attr">click</span>)=<span class="hljs-string">"logout()"</span>></span> Logout<span class="hljs-tag">&#x3C;/<span class="hljs-name">button</span>></span>
 <span class="hljs-tag">&#x3C;/<span class="hljs-name">div</span>></span>
  <span class="hljs-comment">&#x3C;!-- User NOT logged in --></span>
  <span class="hljs-tag">&#x3C;<span class="hljs-name">div</span> *<span class="hljs-attr">ngIf</span>=<span class="hljs-string">"!auth.currentUser"</span>></span>
    <span class="hljs-tag">&#x3C;<span class="hljs-name">h3</span>></span>Hi there, {{ auth.currentUserDisplayName }}<span class="hljs-tag">&#x3C;/<span class="hljs-name">h3</span>></span>
    <span class="hljs-tag">&#x3C;<span class="hljs-name">p</span>></span>Login to get started...<span class="hljs-tag">&#x3C;/<span class="hljs-name">p</span>></span>
    <span class="hljs-tag">&#x3C;<span class="hljs-name">button</span> <span class="hljs-attr">md-raised-button</span> <span class="hljs-attr">color</span>=<span class="hljs-string">"primary"</span> <span class="hljs-attr">routerLink</span>=<span class="hljs-string">"/login"</span>></span>Login<span class="hljs-tag">&#x3C;/<span class="hljs-name">button</span>></span>
  <span class="hljs-tag">&#x3C;/<span class="hljs-name">div</span>></span></code></pre>
<p>This is a bare bones profile page that simply displays the current user's profile picture if provided or else sets a default if undefined and a logout button to sign-out of our account.</p>
<p>If the user isn't logged in while navigating to this page, we ask them nicely to login. To prevent this from happening we could use an ngIf in our navbar to only show this when users are logged in, but it could be a wise design choice to show our potential users the pages they could have access to if they sign-in.</p>
<p>Some pages however we never want unauthorized users to be able to access. These pages definitely should be conditionally rendered in our navbar AND guarded against access with our route guard we'll construct below.</p>
<h1 id="step-6-guarding-authorized-routes">Step 6: Guarding Authorized Routes</h1>
<p>First we need to create the guard file. We'll use Angular's CLI to generate the file.</p>
<pre><code class="hljs language-console"><span class="hljs-attribute">ng</span> g guard core/auth</code></pre>
<p>Now we need to bootstrap it as a provider in our app.module file just like any other injectable service.</p>
<h4 id="appmodulets-3">app.module.ts</h4>
<pre><code class="hljs language-ts"><span class="hljs-keyword">import</span> { AuthService } from <span class="hljs-string">'./core/auth.service'</span>;
<span class="hljs-keyword">import</span> { AuthGuard} from <span class="hljs-string">'./core/auth.guard'</span>;
<span class="hljs-comment">// ... omitted</span>
  providers: [
    AuthService,
    AuthGuard,
  ],</code></pre>
<p>Now we can use the authenticated getter function we defined in our auth service made earlier to check if a user is logged in or not.</p>
<h4 id="authguardts">auth.guard.ts</h4>
<pre><code class="hljs language-ts"><span class="hljs-keyword">import</span> { Injectable } from <span class="hljs-string">'@angular/core'</span>;
<span class="hljs-keyword">import</span> {
  CanActivate,
  ActivatedRouteSnapshot,
  RouterStateSnapshot,
  Router } from <span class="hljs-string">'@angular/router'</span>;
<span class="hljs-keyword">import</span> { Observable } from <span class="hljs-string">'rxjs/Observable'</span>;
<span class="hljs-keyword">import</span> { AuthService } from <span class="hljs-string">'./auth.service'</span>;
<span class="hljs-keyword">import</span> <span class="hljs-string">'rxjs/add/operator/do'</span>;
<span class="hljs-keyword">import</span> <span class="hljs-string">'rxjs/add/operator/map'</span>;
<span class="hljs-keyword">import</span> <span class="hljs-string">'rxjs/add/operator/take'</span>;

<span class="hljs-meta">@Injectable</span>()
<span class="hljs-keyword">export</span> <span class="hljs-keyword">class</span> AuthGuard <span class="hljs-keyword">implements</span> CanActivate {
  <span class="hljs-keyword">constructor</span>(<span class="hljs-params"><span class="hljs-keyword">private</span> auth: AuthService, <span class="hljs-keyword">private</span> router: Router</span>) {}

  canActivate(
    next: ActivatedRouteSnapshot,
    state: RouterStateSnapshot): Observable&#x3C;<span class="hljs-built_in">any</span>> | <span class="hljs-built_in">boolean</span> {
      <span class="hljs-keyword">if</span> (<span class="hljs-keyword">this</span>.auth.authenticated) { <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>; }

      <span class="hljs-keyword">return</span> <span class="hljs-keyword">this</span>.auth.currentUserObservable
         .take(<span class="hljs-number">1</span>)
         .map(user => !!user)
         .do(loggedIn => {
           <span class="hljs-keyword">if</span> (!loggedIn) {
             <span class="hljs-built_in">console</span>.log(<span class="hljs-string">"access denied"</span>)
             <span class="hljs-keyword">this</span>.router.navigate([<span class="hljs-string">'/login'</span>]);
           }
         })

  }
}</code></pre>
<p>Here we set up an observable to subscribe to the login state of our users and use the CanActivate function to specify the route we're activating with 'ActivatedRouteSnapshot', if we have a current user we return true and the canActivate function allows access to our route, if false we use the RxJS do function to catch unauthenticated users and redirect them to the login page and log "access denied" this could be easily replaced with a toast message or similar alert.</p>
<h3 id="guarding-router-routes">Guarding Router Routes</h3>
<p>Now that we have our general purpose guard set up, we can specify which routes it should guard from unauthenticated users.</p>
<h4 id="app-routingmodulets">app-routing.module.ts</h4>
<pre><code class="hljs language-ts"><span class="hljs-keyword">import</span> { AuthGuard } from <span class="hljs-string">'./core/auth.guard'</span>;
<span class="hljs-keyword">const</span> routes: Routes = [
  { path: <span class="hljs-string">'items'</span>, component: SomeComponent, canActivate: [AuthGuard]},
]</code></pre>
]]></description><link>http://kicz.me/posts/angular-auth/</link><guid isPermaLink="true">http://kicz.me/posts/angular-auth/</guid><pubDate>Tue, 23 May 2017 00:00:00 GMT</pubDate></item><item><title><![CDATA[Wearable Computing and Parkinson's FOG Assistant]]></title><description><![CDATA[<p><img src="https://cdn.instructables.com/FUK/CTU1/I9CLX032/FUKCTU1I9CLX032.MEDIUM.jpg" alt="power modules"></p>
<iframe width="560" height="315" src="https://www.youtube.com/embed/Pmr8eylvb8s?rel=0" frameborder="0" allowfullscreen></iframe>
<p>Update: This was a short project I undertook, which lasted just over a month. There's a lot I would change, from the design of the battery modules (switching to higher energy density 18650 LiMnCo cells) to improvements of the code. This tutorial is still relevant as a reference for what's possible through rapid prototyping and as a starting point for anyone working on wearables or gait analysis.</p>
<p>Many useful applications for the cheap processing power and sensors we have today are limited by the short duration of use provided by our batteries. To solve this, slim LiPo battery modules were made that attach to a user’s belt with a simple clip on the back. Each module contains a LiPo cell, LiPo protection circuit (PCM), and magnetic power terminals to allow them to be easily snapped into chains to provide more power. These power modules are linked in parallel to a control module that contains a step-up regulator with integrated USB to allow it to charge a phone or other devices, power terminals for powering loads with non-standard connections, and an internal LiPo charger with micro-USB connection for convenient charging individually or as a chain.</p>
<p>As a demonstration of the possibilities these modules open up, we decided to focus on creating a wearable device that enables people diagnosed with Parkinson’s disease (PD) to escape Freeze of Gait (FOG) moments and improve their mobility and independence. To do this, a gyroscope constantly monitors a patient’s motion to identify an incidence of slowed or frozen gait. In the case of a freeze, a laser diode with a line projection lens shines a laser-line in front of a user so that they have a visual cue to step forward, replacing the cueing function of a PD patient’s impaired Basal ganglia. Similar techniques have successfully been used to reduce FoG moments and improve walking velocity.</p>
<h1 id="step-1-materials-and-tools">Step 1: Materials and Tools</h1>
<h3 id="power-module-parts">Power Module Parts:</h3>
<ul>
<li>10x10x2mm Magnets</li>
</ul>
<p>These strong neodymium magnets were used to allow the modules to easily snap together in parallel.</p>
<ul>
<li>3.7v LiPo Battery Packs</li>
</ul>
<p>We used these 4S LiPo packs from Turnigy because they were the right dimensions for the job, cost-effective per cell, high performance, and can be easy broken apart into four 3.7v cells with 850mah each.</p>
<ul>
<li>5V Boost Switching Linear Regulator w/ USB</li>
</ul>
<p>1A output Boosting DC-DC converter from ebay to boost up the batteries from an average voltage of 3.7V to 5V to drive the Arduino or charge a phone.</p>
<ul>
<li>LiPo Charger</li>
</ul>
<p>These LiPo chargers are perfect for conveniently charging the 3.7v cells with up to 1A current from supported USB ports.</p>
<ul>
<li>LiPo PCMs</li>
</ul>
<p>Pretty much any PCM designed for 3.7v LiPos will work, but you want to make sure that they have over-charge (OC), over-discharge (OD), and short circuit (SC) protection. We used these modules from ebay.</p>
<ul>
<li>Copper tape for wiring to magnets</li>
</ul>
<p>These were found locally from a hardware store with a diverse inventory called McGuckins. Unfortunately Home Depot does not carry the right kind of copper tape.</p>
<ul>
<li>Adhesive</li>
</ul>
<p>Gorilla glue epoxy is pretty great.</p>
<ul>
<li>Screws 3.6mm Length x 2.2mm Diameter</li>
</ul>
<p>We also found these locally, unfortunately Home Depot is unlikely to carry screws this small.</p>
<h3 id="gait-module-parts">Gait Module Parts:</h3>
<p>Minimum parts:</p>
<ul>
<li>Arduino Pro Mini/Micro</li>
</ul>
<p>We used an Arduino Pro Micro from Sparkfun.</p>
<ul>
<li>
<p>Accelerometer/Gyroscope MPU 6050</p>
</li>
<li>
<p>Laser diode with a lens that projects a line</p>
</li>
</ul>
<p>These laser diodes with a lens to focus the beam into a line were also found on ebay.</p>
<ul>
<li>A mini-breadboard for easy connections to the Arduino.</li>
</ul>
<p>We found 5 of these little cuties on eBay.</p>
<h3 id="belt-or-waist-attachment-parts">Belt or Waist attachment Parts:</h3>
<ul>
<li>These modules can use either an elastic or adjustable strap as well as metal clips so that they can be attached to the waist without a belt. We used these bail making pliers to bend 3003 aluminum strips into clips for the modules.</li>
</ul>
<h3 id="parts-for-improvements">Parts for improvements:</h3>
<ul>
<li>
<p>Through-hole potentiometer for gait-detection sensitivity adjustment.</p>
</li>
<li>
<p>Through-hole RGB LED for feedback.</p>
</li>
<li>
<p>Buzzer motor or solenoid for haptic feedback and gait-pace setting.</p>
</li>
</ul>
<h3 id="other-tools">Other Tools:</h3>
<ul>
<li>
<p>Multimeter for testing</p>
</li>
<li>
<p>Soldering iron and helping hands (clips for holding parts)</p>
</li>
<li>
<p>Small phillips screw driver</p>
</li>
<li>
<p>Millimeter precision measuring calipers were very useful for this project, but not necessary if you plan to use all of our designs and parts.</p>
</li>
</ul>
<p><img src="https://cdn.instructables.com/FE7/87VN/I96W83CS/FE787VNI96W83CS.MEDIUM.jpg?width=614" alt="circuit diagram"></p>
<p><img src="https://cdn.instructables.com/F5R/I8A3/I96W8E89/F5RI8A3I96W8E89.MEDIUM.jpg" alt="visual circuit connections"></p>
<h1 id="step-2-build-breadboard-circuit">Step 2: Build Breadboard Circuit</h1>
<p>This schematic shows how to connect the MPU 6050 gyroscope + accelerometer, the laser diode with focus line, and the Arduino pro mini. If you are using a different micro-controller, check which pins your board uses for the I2C pins; they can vary significantly between Arduino models.</p>
<p><img src="https://cdn.instructables.com/F2N/EH5G/I9EP24V0/F2NEH5GI9EP24V0.MEDIUM.jpg?width=614" alt="Roll pitch and yaw diagram"></p>
<h1 id="step-3-understanding-the-gyroscope">Step 3: Understanding the Gyroscope</h1>
<p>We began by downloading a preexisting library for the MPU 6050(gyroscope/potentiometer). We downloaded the library called MPU6050_DMP6 found on the following tutorial:</p>
<p><a href="http://diyhacking.com/arduino-mpu-6050-imu-sensor-..">http://diyhacking.com/arduino-mpu-6050-imu-sensor-..</a>.</p>
<p>This tutorial helped us to understand how our potentiometer and gyroscope picked up data. Through the serial port, we learned the data values for yaw, pitch and roll.</p>
<p>For us, understanding how the movements of the body relate to yaw, pitch, and roll values and how to interpret those values were the biggest challenges. To better visualize yaw, pitch, and roll, we've attached a useful diagram above.</p>
<h1 id="step-4-arduino-code">Step 4: Arduino Code</h1>
<p>As mentioned earlier, our first step was to understand the yaw, pitch, and roll values from the gyroscope/accelerometer. We observed that the yaw values from the gyroscope were the biggest identifier of a walking gait. We created a custom algorithm that counted the deviations from the computed average yaw value to the left and right as they occurred when taking a step with right and left legs respectively. When the L and R count values were near a 1/1 ratio of the average we determined that walking had occurred. We then wrote more logic to detect when walking stopped after previous walking had been detected and then fired the laser (an LED while testing). Not the most accurate method, but it worked pretty well for the most part and was a simple routine that performed well on an arduino mini.</p>
<pre><code class="hljs language-Arduino"><span class="hljs-comment">// I2Cdev and MPU6050 must be installed as libraries, or else the .cpp/.h files</span>
<span class="hljs-comment">// for both classes must be in the include path of your project</span>
<span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">"I2Cdev.h"</span></span>

<span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">"MPU6050_6Axis_MotionApps20.h"</span></span>
<span class="hljs-comment">//#include "MPU6050.h" // not necessary if using MotionApps include file</span>

<span class="hljs-comment">// Arduino Wire library is required if I2Cdev I2CDEV_ARDUINO_WIRE implementation</span>
<span class="hljs-comment">// is used in I2Cdev.h</span>
<span class="hljs-meta">#<span class="hljs-meta-keyword">if</span> I2CDEV_IMPLEMENTATION == I2CDEV_ARDUINO_WIRE</span>
    <span class="hljs-meta">#<span class="hljs-meta-keyword">include</span> <span class="hljs-meta-string">"Wire.h"</span></span>
<span class="hljs-meta">#<span class="hljs-meta-keyword">endif</span></span>

<span class="hljs-comment">// class default I2C address is 0x68</span>
<span class="hljs-comment">// specific I2C addresses may be passed as a parameter here</span>
<span class="hljs-comment">// AD0 low = 0x68 (default for SparkFun breakout and InvenSense evaluation board)</span>
<span class="hljs-comment">// AD0 high = 0x69</span>
MPU6050 mpu;



<span class="hljs-meta">#<span class="hljs-meta-keyword">define</span> LED_PIN 13 <span class="hljs-comment">// (Arduino is 13, Teensy is 11, Teensy++ is 6)</span></span>
<span class="hljs-keyword">bool</span> blinkState = false;

<span class="hljs-comment">// MPU control/status vars</span>
<span class="hljs-keyword">bool</span> dmpReady = false;  <span class="hljs-comment">// set true if DMP init was successful</span>
uint8_t mpuIntStatus;   <span class="hljs-comment">// holds actual interrupt status byte from MPU</span>
uint8_t devStatus;      <span class="hljs-comment">// return status after each device operation (0 = success, !0 = error)</span>
uint16_t packetSize;    <span class="hljs-comment">// expected DMP packet size (default is 42 bytes)</span>
uint16_t fifoCount;     <span class="hljs-comment">// count of all bytes currently in FIFO</span>
uint8_t fifoBuffer[<span class="hljs-number">64</span>]; <span class="hljs-comment">// FIFO storage buffer</span>

<span class="hljs-comment">// orientation/motion vars</span>
Quaternion q;           <span class="hljs-comment">// [w, x, y, z]         quaternion container</span>
VectorInt16 aa;         <span class="hljs-comment">// [x, y, z]            accel sensor measurements</span>
VectorInt16 aaReal;     <span class="hljs-comment">// [x, y, z]            gravity-free accel sensor measurements</span>
VectorInt16 aaWorld;    <span class="hljs-comment">// [x, y, z]            world-frame accel sensor measurements</span>
VectorFloat gravity;    <span class="hljs-comment">// [x, y, z]            gravity vector</span>
<span class="hljs-keyword">float</span> euler[<span class="hljs-number">3</span>];         <span class="hljs-comment">// [psi, theta, phi]    Euler angle container</span>
<span class="hljs-keyword">float</span> ypr[<span class="hljs-number">3</span>];           <span class="hljs-comment">// [yaw, pitch, roll]   yaw/pitch/roll container and gravity vector</span>

<span class="hljs-comment">// packet structure for InvenSense teapot demo</span>
uint8_t teapotPacket[<span class="hljs-number">14</span>] = { <span class="hljs-string">'$'</span>, <span class="hljs-number">0x02</span>, <span class="hljs-number">0</span>,<span class="hljs-number">0</span>, <span class="hljs-number">0</span>,<span class="hljs-number">0</span>, <span class="hljs-number">0</span>,<span class="hljs-number">0</span>, <span class="hljs-number">0</span>,<span class="hljs-number">0</span>, <span class="hljs-number">0x00</span>, <span class="hljs-number">0x00</span>, <span class="hljs-string">'\r'</span>, <span class="hljs-string">'\n'</span> };

<span class="hljs-comment">//Other Variables</span>
<span class="hljs-keyword">int</span> led = <span class="hljs-number">8</span>;
<span class="hljs-keyword">int</span> stabilize = <span class="hljs-number">0</span>;
<span class="hljs-keyword">const</span> <span class="hljs-keyword">int</span> numReadings = <span class="hljs-number">200</span>;
<span class="hljs-keyword">int</span> yawSample[numReadings];
<span class="hljs-keyword">int</span> index = <span class="hljs-number">0</span>;
<span class="hljs-keyword">int</span> total = <span class="hljs-number">0</span>;
<span class="hljs-keyword">int</span> aveYaw = <span class="hljs-number">0</span>;
<span class="hljs-keyword">int</span> minYaw = <span class="hljs-number">1000</span>;
<span class="hljs-keyword">int</span> maxYaw = <span class="hljs-number">0</span>;
<span class="hljs-keyword">int</span> difYaw = <span class="hljs-number">0</span>;
<span class="hljs-keyword">int</span> oldDifYaw = <span class="hljs-number">0</span>;
<span class="hljs-keyword">int</span> iteration = <span class="hljs-number">0</span>;
<span class="hljs-keyword">int</span> butPin = <span class="hljs-number">4</span>;
<span class="hljs-keyword">int</span> butState = <span class="hljs-number">0</span>;
<span class="hljs-keyword">int</span> initIter = <span class="hljs-number">0</span>;
<span class="hljs-keyword">float</span> posCount = <span class="hljs-number">0</span>;
<span class="hljs-keyword">float</span> negCount = <span class="hljs-number">0</span>;
<span class="hljs-keyword">boolean</span> wasWalking = false;
<span class="hljs-keyword">int</span> potPin = A0;



<span class="hljs-comment">// ================================================================</span>
<span class="hljs-comment">// ===               INTERRUPT DETECTION ROUTINE                ===</span>
<span class="hljs-comment">// ================================================================</span>

<span class="hljs-keyword">volatile</span> <span class="hljs-keyword">bool</span> mpuInterrupt = false;     <span class="hljs-comment">// indicates whether MPU interrupt pin has gone high</span>
<span class="hljs-keyword">void</span> dmpDataReady() {
    mpuInterrupt = true;
}



<span class="hljs-comment">// ================================================================</span>
<span class="hljs-comment">// ===                      INITIAL SETUP                       ===</span>
<span class="hljs-comment">// ================================================================</span>

<span class="hljs-keyword">void</span> <span class="hljs-built_in">setup</span>() {
    <span class="hljs-comment">// join I2C bus (I2Cdev library doesn't do this automatically)</span>
    <span class="hljs-meta">#<span class="hljs-meta-keyword">if</span> I2CDEV_IMPLEMENTATION == I2CDEV_ARDUINO_WIRE</span>
        <span class="hljs-built_in">Wire</span>.<span class="hljs-built_in">begin</span>();
        TWBR = <span class="hljs-number">24</span>; <span class="hljs-comment">// 400kHz I2C clock (200kHz if CPU is 8MHz)</span>
    <span class="hljs-meta">#<span class="hljs-meta-keyword">elif</span> I2CDEV_IMPLEMENTATION == I2CDEV_BUILTIN_FASTWIRE</span>
        Fastwire::<span class="hljs-built_in">setup</span>(<span class="hljs-number">400</span>, true);
    <span class="hljs-meta">#<span class="hljs-meta-keyword">endif</span></span>

    <span class="hljs-comment">// initialize serial communication</span>
    <span class="hljs-comment">// (115200 chosen because it is required for Teapot Demo output, but it's</span>
    <span class="hljs-comment">// really up to you depending on your project)</span>
    <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">begin</span>(<span class="hljs-number">115200</span>);
    <span class="hljs-built_in">while</span> (!<span class="hljs-built_in">Serial</span>); <span class="hljs-comment">// wait for Leonardo enumeration, others continue immediately</span>

    <span class="hljs-comment">// <span class="hljs-doctag">NOTE:</span> 8MHz or slower host processors, like the Teensy @ 3.3v or Ardunio</span>
    <span class="hljs-comment">// Pro Mini running at 3.3v, cannot handle this baud rate reliably due to</span>
    <span class="hljs-comment">// the baud timing being too misaligned with processor ticks. You must use</span>
    <span class="hljs-comment">// 38400 or slower in these cases, or use some kind of external separate</span>
    <span class="hljs-comment">// crystal solution for the UART timer.</span>

    <span class="hljs-comment">// initialize device</span>
    <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">println</span>(F(<span class="hljs-string">"Initializing I2C devices..."</span>));
    mpu.initialize();

    <span class="hljs-comment">// verify connection</span>
    <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">println</span>(F(<span class="hljs-string">"Testing device connections..."</span>));
    <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">println</span>(mpu.testConnection() ? F(<span class="hljs-string">"MPU6050 connection successful"</span>) : F(<span class="hljs-string">"MPU6050 connection failed"</span>));

    <span class="hljs-comment">// load and configure the DMP</span>
    <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">println</span>(F(<span class="hljs-string">"Initializing DMP..."</span>));
    devStatus = mpu.dmpInitialize();

    <span class="hljs-comment">// supply your own gyro offsets here, scaled for min sensitivity</span>
    mpu.setXGyroOffset(<span class="hljs-number">220</span>);
    mpu.setYGyroOffset(<span class="hljs-number">76</span>);
    mpu.setZGyroOffset(<span class="hljs-number">-85</span>);
    mpu.setZAccelOffset(<span class="hljs-number">1788</span>); <span class="hljs-comment">// 1688 factory default for my test chip</span>

    <span class="hljs-comment">// make sure it worked (returns 0 if so)</span>
    <span class="hljs-built_in">if</span> (devStatus == <span class="hljs-number">0</span>) {
        <span class="hljs-comment">// turn on the DMP, now that it's ready</span>
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">println</span>(F(<span class="hljs-string">"Enabling DMP..."</span>));
        mpu.setDMPEnabled(true);

        <span class="hljs-comment">// enable Arduino interrupt detection</span>
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">println</span>(F(<span class="hljs-string">"Enabling interrupt detection (Arduino external interrupt 0)..."</span>));
        <span class="hljs-built_in">attachInterrupt</span>(<span class="hljs-number">0</span>, dmpDataReady, RISING);
        mpuIntStatus = mpu.getIntStatus();

        <span class="hljs-comment">// set our DMP Ready flag so the main loop() function knows it's okay to use it</span>
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">println</span>(F(<span class="hljs-string">"DMP ready! Waiting for first interrupt..."</span>));
        dmpReady = true;

        <span class="hljs-comment">// get expected DMP packet size for later comparison</span>
        packetSize = mpu.dmpGetFIFOPacketSize();
    } <span class="hljs-built_in">else</span> {
        <span class="hljs-comment">// ERROR!</span>
        <span class="hljs-comment">// 1 = initial memory load failed</span>
        <span class="hljs-comment">// 2 = DMP configuration updates failed</span>
        <span class="hljs-comment">// (if it's going to break, usually the code will be 1)</span>
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(F(<span class="hljs-string">"DMP Initialization failed (code "</span>));
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(devStatus);
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">println</span>(F(<span class="hljs-string">")"</span>));
    }

    <span class="hljs-comment">// configure LED for output</span>
    <span class="hljs-built_in">pinMode</span>(LED_PIN, <span class="hljs-literal">OUTPUT</span>);
    <span class="hljs-built_in">pinMode</span>(led, <span class="hljs-literal">OUTPUT</span>);
    <span class="hljs-built_in">pinMode</span>(butPin, <span class="hljs-literal">INPUT</span>);
    <span class="hljs-built_in">pinMode</span>(potPin, <span class="hljs-literal">INPUT</span>);

    <span class="hljs-comment">//initialize all the readings to 0:</span>
    <span class="hljs-built_in">for</span>(<span class="hljs-keyword">int</span> thisReading = <span class="hljs-number">0</span>; thisReading &#x3C; numReadings; thisReading++) yawSample[thisReading] = <span class="hljs-number">0</span>;
}



<span class="hljs-comment">// ================================================================</span>
<span class="hljs-comment">// ===                    MAIN PROGRAM LOOP                     ===</span>
<span class="hljs-comment">// ================================================================</span>

<span class="hljs-keyword">void</span> <span class="hljs-built_in">loop</span>() {
    <span class="hljs-comment">// if programming failed, don't try to do anything</span>
    <span class="hljs-built_in">if</span> (!dmpReady) <span class="hljs-built_in">return</span>;

    <span class="hljs-comment">// wait for MPU interrupt or extra packet(s) available</span>
    <span class="hljs-built_in">while</span> (!mpuInterrupt &#x26;&#x26; fifoCount &#x3C; packetSize) {
        <span class="hljs-comment">// other program behavior stuff here</span>
        <span class="hljs-comment">// .</span>
        <span class="hljs-comment">// .</span>
        <span class="hljs-comment">// .</span>
        <span class="hljs-comment">// if you are really paranoid you can frequently test in between other</span>
        <span class="hljs-comment">// stuff to see if mpuInterrupt is true, and if so, "break;" from the</span>
        <span class="hljs-comment">// while() loop to immediately process the MPU data</span>
        <span class="hljs-comment">// .</span>
        <span class="hljs-comment">// .</span>
        <span class="hljs-comment">// .</span>
    }

    <span class="hljs-comment">// reset interrupt flag and get INT_STATUS byte</span>
    mpuInterrupt = false;
    mpuIntStatus = mpu.getIntStatus();

    <span class="hljs-comment">// get current FIFO count</span>
    fifoCount = mpu.getFIFOCount();

    <span class="hljs-comment">// check for overflow (this should never happen unless our code is too inefficient)</span>
    <span class="hljs-built_in">if</span> ((mpuIntStatus &#x26; <span class="hljs-number">0x10</span>) || fifoCount == <span class="hljs-number">1024</span>) {
        <span class="hljs-comment">// reset so we can continue cleanly</span>
        mpu.resetFIFO();
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">println</span>(F(<span class="hljs-string">"FIFO overflow!"</span>));

    <span class="hljs-comment">// otherwise, check for DMP data ready interrupt (this should happen frequently)</span>
    } <span class="hljs-built_in">else</span> <span class="hljs-built_in">if</span> (mpuIntStatus &#x26; <span class="hljs-number">0x02</span>) {
        <span class="hljs-comment">// wait for correct available data length, should be a VERY short wait</span>
        <span class="hljs-built_in">while</span> (fifoCount &#x3C; packetSize) fifoCount = mpu.getFIFOCount();

        <span class="hljs-comment">// read a packet from FIFO</span>
        mpu.getFIFOBytes(fifoBuffer, packetSize);

        <span class="hljs-comment">// track FIFO count here in case there is > 1 packet available</span>
        <span class="hljs-comment">// (this lets us immediately read more without waiting for an interrupt)</span>
        fifoCount -= packetSize;

        <span class="hljs-meta">#<span class="hljs-meta-keyword">ifdef</span> OUTPUT_READABLE_QUATERNION</span>
            <span class="hljs-comment">// display quaternion values in easy matrix form: w x y z</span>
            mpu.dmpGetQuaternion(&#x26;q, fifoBuffer);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"quat\t"</span>);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(q.w);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"\t"</span>);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(q.x);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"\t"</span>);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(q.y);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"\t"</span>);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">println</span>(q.z);
        <span class="hljs-meta">#<span class="hljs-meta-keyword">endif</span></span>

        <span class="hljs-meta">#<span class="hljs-meta-keyword">ifdef</span> OUTPUT_READABLE_EULER</span>
            <span class="hljs-comment">// display Euler angles in degrees</span>
            mpu.dmpGetQuaternion(&#x26;q, fifoBuffer);
            mpu.dmpGetEuler(euler, &#x26;q);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"euler\t"</span>);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(euler[<span class="hljs-number">0</span>] * <span class="hljs-number">180</span>/M_PI);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"\t"</span>);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(euler[<span class="hljs-number">1</span>] * <span class="hljs-number">180</span>/M_PI);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"\t"</span>);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">println</span>(euler[<span class="hljs-number">2</span>] * <span class="hljs-number">180</span>/M_PI);
        <span class="hljs-meta">#<span class="hljs-meta-keyword">endif</span></span>

        <span class="hljs-meta">#<span class="hljs-meta-keyword">ifdef</span> OUTPUT_READABLE_YAWPITCHROLL</span>
            <span class="hljs-comment">// display Euler angles in degrees</span>
            mpu.dmpGetQuaternion(&#x26;q, fifoBuffer);
            mpu.dmpGetGravity(&#x26;gravity, &#x26;q);
            mpu.dmpGetYawPitchRoll(ypr, &#x26;q, &#x26;gravity);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"ypr\t"</span>);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(ypr[<span class="hljs-number">0</span>] * <span class="hljs-number">180</span>/M_PI);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"\t"</span>);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(ypr[<span class="hljs-number">1</span>] * <span class="hljs-number">180</span>/M_PI);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"\t"</span>);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">println</span>(ypr[<span class="hljs-number">2</span>] * <span class="hljs-number">180</span>/M_PI);
        <span class="hljs-meta">#<span class="hljs-meta-keyword">endif</span></span>

        <span class="hljs-meta">#<span class="hljs-meta-keyword">ifdef</span> OUTPUT_READABLE_REALACCEL</span>
            <span class="hljs-comment">// display real acceleration, adjusted to remove gravity</span>
            mpu.dmpGetQuaternion(&#x26;q, fifoBuffer);
            mpu.dmpGetAccel(&#x26;aa, fifoBuffer);
            mpu.dmpGetGravity(&#x26;gravity, &#x26;q);
            mpu.dmpGetLinearAccel(&#x26;aaReal, &#x26;aa, &#x26;gravity);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"areal\t"</span>);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(aaReal.x);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"\t"</span>);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(aaReal.y);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"\t"</span>);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">println</span>(aaReal.z);
        <span class="hljs-meta">#<span class="hljs-meta-keyword">endif</span></span>

        <span class="hljs-meta">#<span class="hljs-meta-keyword">ifdef</span> OUTPUT_READABLE_WORLDACCEL</span>
            <span class="hljs-comment">// display initial world-frame acceleration, adjusted to remove gravity</span>
            <span class="hljs-comment">// and rotated based on known orientation from quaternion</span>
            mpu.dmpGetQuaternion(&#x26;q, fifoBuffer);
            mpu.dmpGetAccel(&#x26;aa, fifoBuffer);
            mpu.dmpGetGravity(&#x26;gravity, &#x26;q);
            mpu.dmpGetLinearAccel(&#x26;aaReal, &#x26;aa, &#x26;gravity);
            mpu.dmpGetLinearAccelInWorld(&#x26;aaWorld, &#x26;aaReal, &#x26;q);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"aworld\t"</span>);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(aaWorld.x);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"\t"</span>);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(aaWorld.y);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"\t"</span>);
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">println</span>(aaWorld.z);
        <span class="hljs-meta">#<span class="hljs-meta-keyword">endif</span></span>

        <span class="hljs-meta">#<span class="hljs-meta-keyword">ifdef</span> OUTPUT_TEAPOT</span>
            <span class="hljs-comment">// display quaternion values in InvenSense Teapot demo format:</span>
            teapotPacket[<span class="hljs-number">2</span>] = fifoBuffer[<span class="hljs-number">0</span>];
            teapotPacket[<span class="hljs-number">3</span>] = fifoBuffer[<span class="hljs-number">1</span>];
            teapotPacket[<span class="hljs-number">4</span>] = fifoBuffer[<span class="hljs-number">4</span>];
            teapotPacket[<span class="hljs-number">5</span>] = fifoBuffer[<span class="hljs-number">5</span>];
            teapotPacket[<span class="hljs-number">6</span>] = fifoBuffer[<span class="hljs-number">8</span>];
            teapotPacket[<span class="hljs-number">7</span>] = fifoBuffer[<span class="hljs-number">9</span>];
            teapotPacket[<span class="hljs-number">8</span>] = fifoBuffer[<span class="hljs-number">12</span>];
            teapotPacket[<span class="hljs-number">9</span>] = fifoBuffer[<span class="hljs-number">13</span>];
            <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">write</span>(teapotPacket, <span class="hljs-number">14</span>);
            teapotPacket[<span class="hljs-number">11</span>]++; <span class="hljs-comment">// packetCount, loops at 0xFF on purpose</span>
        <span class="hljs-meta">#<span class="hljs-meta-keyword">endif</span></span>

        <span class="hljs-comment">// blink LED to indicate activity</span>
        <span class="hljs-comment">//blinkState = !blinkState;</span>
        <span class="hljs-comment">//digitalWrite(LED_PIN, blinkState);</span>
    }

    <span class="hljs-built_in">if</span>(stabilize &#x3C; <span class="hljs-number">1400</span>) {
      stabilize++;
      <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"Stabilizing Sensor Values"</span>);
    }

    <span class="hljs-built_in">if</span>(stabilize >= <span class="hljs-number">1400</span>) {
      iteration++;
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">println</span>(<span class="hljs-string">"IPNAMiMaOD: "</span>);
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(iteration);
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"  "</span>);
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(posCount);
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"  "</span>);
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(negCount);
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"  "</span>);
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(aveYaw);
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"  "</span>);
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(minYaw);
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"  "</span>);
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(maxYaw);
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"  "</span>);
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(oldDifYaw);
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"  "</span>);
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(difYaw);
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"  "</span>);
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"Hello Pot"</span>);
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-built_in">analogRead</span>(potPin));
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">print</span>(<span class="hljs-string">"  "</span>);

      <span class="hljs-comment">// Calculate average sensor values</span>
      total = total - yawSample[index];
      yawSample[index] = (ypr[<span class="hljs-number">0</span>] * (<span class="hljs-number">180</span>/M_PI));
      total = total + yawSample[index];
      index = index + <span class="hljs-number">1</span>;
      <span class="hljs-comment">//if at the end of the array, wrap around to the beginning</span>
      <span class="hljs-built_in">if</span>(index >= numReadings) index = <span class="hljs-number">0</span>;

      minYaw = <span class="hljs-built_in">min</span>(minYaw, yawSample[index]);
      maxYaw = <span class="hljs-built_in">max</span>(maxYaw, yawSample[index]);
      aveYaw = total / numReadings;

      butState = <span class="hljs-built_in">digitalRead</span>(butPin);
      <span class="hljs-comment">//Serial.print("butPin");</span>
      <span class="hljs-comment">//Serial.print(butState);</span>

      <span class="hljs-comment">// if walking has stopped after walking has been detected fire the laser</span>
      <span class="hljs-built_in">if</span>(!walking() &#x26;&#x26; wasWalking &#x26;&#x26; iteration > <span class="hljs-number">200</span>) {
        <span class="hljs-built_in">digitalWrite</span>(led, <span class="hljs-literal">HIGH</span>);
      }   
      <span class="hljs-built_in">if</span>(walking()) {
        <span class="hljs-built_in">Serial</span>.<span class="hljs-built_in">println</span>(<span class="hljs-string">"WALKING DETECTED"</span>);
        initIter = iteration;
        <span class="hljs-comment">//digitalWrite(led, LOW);</span>
        wasWalking = true;
      }
      <span class="hljs-built_in">if</span>(iteration > (initIter + <span class="hljs-number">100</span>)) {
        <span class="hljs-built_in">digitalWrite</span>(led, <span class="hljs-literal">LOW</span>);
      }
      <span class="hljs-built_in">if</span>(iteration > <span class="hljs-number">200</span>) {
        difYaw = <span class="hljs-built_in">abs</span>(maxYaw - minYaw);
      }
      <span class="hljs-comment">// reset our values every 500 iterations</span>
      <span class="hljs-built_in">if</span>(iteration > <span class="hljs-number">500</span>) {
        iteration = <span class="hljs-number">0</span>;
        posCount = <span class="hljs-number">0</span>;
        negCount = <span class="hljs-number">0</span>;
        minYaw = aveYaw;
        maxYaw = aveYaw;
        oldDifYaw = difYaw;
        initIter = <span class="hljs-number">0</span>;
      }
    }  
}

<span class="hljs-comment">// detects walking gait using deviation from average yaw</span>
<span class="hljs-keyword">boolean</span> walking () {
  <span class="hljs-built_in">if</span>(ypr[<span class="hljs-number">0</span>] * (<span class="hljs-number">180</span>/M_PI) >= (aveYaw + <span class="hljs-number">3</span>)) {
    posCount++;
  }
  <span class="hljs-built_in">if</span>(ypr[<span class="hljs-number">0</span>] * (<span class="hljs-number">180</span>/M_PI) &#x3C;= (aveYaw - <span class="hljs-number">3</span>)) {
    negCount++;
  }
  <span class="hljs-built_in">if</span>((posCount > <span class="hljs-number">10</span>) &#x26;&#x26; (negCount > <span class="hljs-number">10</span>)) {
    <span class="hljs-built_in">if</span>(<span class="hljs-number">.5</span> &#x3C; posCount/negCount &#x3C; <span class="hljs-number">1.5</span>) {
      <span class="hljs-built_in">return</span> true;
    }
  }
  <span class="hljs-built_in">else</span> <span class="hljs-built_in">return</span> false;

}</code></pre>
<p>Pretty simple stuff that could use a little refinement, but it works and it's a good starting point for a very useful device. The full code can be seen <a href="https://github.com/Kierancz/gait_project/blob/master/CSCI4830_Gait_Project/CSCI4830_Gait_Project.ino">here</a></p>
<h1 id="step-5-creating-the-power-system">Step 5: Creating the Power System</h1>
<p><img src="https://cdn.instructables.com/FIT/OSM5/I96W8CFC/FITOSM5I96W8CFC.MEDIUM.jpg?width=614" alt="Module shells in AutoCAD"></p>
<p>We designed a whole wearable system that connects LiPo batteries in parallel with other wearable modules in order to power them. The modules consist of a charging/discharging module that is capable of charging external devices through USB or direct connection to output terminals, as well as allowing the battery modules to be charged with a micro-USB cable. Another module consists of an Arduino Micro Pro that serves as a computing module capable of powering all sorts of wearable computing projects. The final module allows a breadboard to easily be combined with the system, enabling rapid prototyping of wearable projects.</p>
<p>The DXG and STI files from AutoCAD can be downloaded from this page.</p>
<p>We had access to a high print quality 3D printer called the Objet, but any 3D printer with at least a .5mm resolution that can also print over-hangs should be able to do an adequate job.</p>
<p>If you do not have access to a 3D printer, easy power systems using a 9V battery and linear or switching regulator (to lower 9V to 5V) can be used to power the Arduino, laser, and gyroscope units. In fact Arduino's often have an on-board regulator that lowers voltages as high as 12v down to the Arduino's operating voltage of 5V. In this case the 9V battery should be attached directly to a pin that's usually labeled RAW. Make sure to check the capabilities of your individual Arduino, so that you don't accidentally supply too much voltage and fry it.</p>
<p>Once the modules have been 3D printed you will need to start building their innards.</p>
<h3 id="step-1-disassemble-4s-lipos">Step 1: Disassemble 4S LiPos</h3>
<p><img src="https://cdn.instructables.com/F5K/4QIV/I9GWD4IX/F5K4QIVI9GWD4IX.LARGE.jpg" alt="LiPo disassembly"></p>
<p><img src="https://cdn.instructables.com/FVI/C3MI/I9GWD4KZ/FVIC3MII9GWD4KZ.LARGE.jpg" alt="LiPo separation with solvent"></p>
<p>The battery packs that we ordered are actually four individual 3.7V battery packs linked in series to create a 14.8V pack. These are stuck together with glue so it's easiest to use a solvent like rubbing alcohol to ease their separation. Once the packs are un-glued, use a soldering iron to take off all the wires protruding from the packs, be carful not to cause any shorts! These pack are high voltage and can cause bright sparks that can shock you or cause a fire! Be careful!! Once you have taken off all the wires, use scissors or wire cutters to cut the tabs holding the cells together, make sure to cut them in the middle to ensure that there's enough tab left to be soldered to.</p>
<h3 id="step-2-solder-on-lipo-pcm">Step 2: Solder on LiPo PCM</h3>
<p>This step is fairly straightforward. Start by folding the battery terminal tabs over into the open area above the battery. Use short wires to connect the battery terminals to the protected terminals on the PCM. These are usually labelled with a B+ and B-. Make sure the wires and PCM are contained within the space at the top of the LiPo cell, otherwise it will be difficult to fit them into the 3D models.</p>
<h3 id="step-3-glue-magnets">Step 3: Glue magnets</h3>
<p>Use the gorilla glue epoxy to set the magnets in their grooves. Make sure that you keep the polarity consistent. One side should be designated as positive polarity and the other negative. For instance we made the right side positive and the left negative. The polarity is the same if they repel each other and opposite if they attract; basic stuff. Make sure that the magnets on the same side repel each other. Once you have figured out how the magnets are to be laid out, put epoxy on the bottom of the magnet grooves of the 3D model and press the magnets into the grooves. Wipe away any epoxy that gets on top of the magnets, the top surface should be clean for good connection to the battery.</p>
<h3 id="step-4-lay-copper-tape">Step 4: Lay copper tape</h3>
<p>Lay down copper tape between the magnets. Since only one side of the tape is conductive due to the adhesive on the back, you will have to fold the ends over so that the copper side without the adhesive is in contact with the magnets. To keep the tape down, apply epoxy to the top of the tape and magnet. Verify that the resistance between the two magnets is not too high ( &#x3C;10 Ohms). After this is completed, add copper tape to the back of the LiPo; lined up so that they will be in contact with the strips in the enclosure. Solder the tape to the protected power terminals of the PCM (labelled P+ and P-).</p>
<h3 id="step-5-put-it-all-together">Step 5: Put it all together</h3>
<p>Insert the battery cell into the model and put the lid on. Drive the screws into the holes in the side to secure the lid. Verify that you have the same voltage across the magnets that you do across the battery. Make sure things are well insulated with electrical tape to prevent shorts.</p>
]]></description><link>http://kicz.me/posts/wearables/</link><guid isPermaLink="true">http://kicz.me/posts/wearables/</guid><pubDate>Thu, 07 May 2015 00:00:00 GMT</pubDate></item><item><title><![CDATA[Origin of an idea]]></title><description><![CDATA[<p>There was nothing magical about getting my first smartphone, the iPhone 5. I remember getting the okay from my parents, the Verizon store setup, the hurried bike ride home, and the tinkering I did on it when I got there. Sure there was plenty of giddy excitement, but none of Apple’s promised magic. That came later when I was able to really use it to connect with the world in new ways. I instantly knew exactly how to get to places I’d never been before, I remembered everything I needed to, I had a photographic memory, and my knowledge seemed almost infinite, I was now superhuman. </p>
<p>I loved it and it became an extension of my body, there was only one problem, it was fragile. I hated the idea of covering up the beautifully sculpted aluminum and glass features in an ugly plastic case. However leaving it naked was not a long-term option, I knew that sharp objects would gouge its soft finish and pocket lint would find a home in its ports and speaker grill. So I started researching. I liked the idea of the various phone sleeves I saw because they offered the protection I sought and they could slide off the phone when it needed to be used. Although there was an obvious flaw, it would be annoying to pull the sleeve out of the pocket, slide the phone out with two hands, and then reverse the process when finished using the device. There were also the extremely dorky phone holsters that bulged out from your belt with very little discretion. These were not acceptable options so I left my phone naked and got designing.</p>
<p>From the beginning I knew it needed to be secured in place with the pouch opening positioned at the top of my pocket so that I could retrieve my phone with one hand. I came up with a system that allowed an adjustable tether system that connected to your belt. Inspired by the prodigious oil wicking ability of the Ipad SmartCover’s microfiber lining, I decided to line the sleeve with microfiber to clean fingerprints from the screen as the phone slid in and out. I also added elastic to the design to allow the pockets to expand to accommodate all of your cards and cash to replace your wallet. Finally I made it modular so it could be further adapted without the need to make new one. After countless sketches, I had my design. I ordered materials, learned how to sew, and started building.</p>
<p>Initially I was quite happy with the finished product, it worked exactly how I had designed it. Then as the months went by I began to notice problems. The tether system was an arduous process that made swapping out the belt annoying. I also noticed from friends testing that it was rather unintuitive. I tried for months to remedy some of the design’s nuisances, but I hated the solutions because they all made the already overcomplicated design even more complex. After about a year of use I decided I needed to start over with renewed focus on the truly important features. This time I knew I needed to drastically shorten each phase of the development and feedback loop; design, build, test, and learn. I needed to rapid prototype ideas, test them with other users, and evaluate and learn from the results. Since that realization, the tether system has become a breeze; it functions better than my wallet, and its more adaptable, beautiful, and easily manufactured than ever.</p>
]]></description><link>http://kicz.me/posts/idea-origin/</link><guid isPermaLink="true">http://kicz.me/posts/idea-origin/</guid><pubDate>Fri, 27 Sep 2013 00:00:00 GMT</pubDate></item></channel></rss>